# Bluey v0.1.2

Overlay placement polish:

- The macOS pill now opens centered by default using the active screen instead of a stale AppKit window screen.
- The expanded overlay recenters when opened, while keeping its current resizable size within the visible screen.
- Daemon status now reports the default overlay position as `center`.
