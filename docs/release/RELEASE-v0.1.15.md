# Bluey v0.1.15

This release publishes the latest overlay, routing, billing, and installer hardening pass.

- Includes the latest click-through/fullscreen, attachment, duplicate-caption, and self-intro routing fixes from the current Bluey branch.
- Includes managed web-search routing, customer-friendly search usage labels, and source-card plumbing.
- Includes billing-risk ledger hardening for credit grants, deductions, refunds/disputes, STT reservation/settlement, sync/RAG blocks, and admin risk review.
- Keeps production macOS overlay capture-hidden; visible overlay flags remain debug-only and are not enabled in shipped binaries.
- Keeps the installer checksum/signed-manifest release flow and the macOS command PATH auto-setup behavior.
