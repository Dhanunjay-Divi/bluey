# Bluey v0.1.4

Release hardening:

- Capture-visible overlay QA flags are now ignored by release macOS overlay binaries.
- The macOS overlay visual smoke now builds a debug overlay explicitly for screenshot-based QA.
- Customer launch paths keep capture exclusion on by default.
