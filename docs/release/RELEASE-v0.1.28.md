# Bluey v0.1.28

- Fixed the macOS Ask input showing two blinking carets.
- Made the signed-out macOS overlay behave like a real sign-in gate: sign-in remains clickable even with click-through enabled, locked controls are disabled, and successful auth returns Bluey to the pill.
- Added visible overlay feedback when Sign in is requested from the native overlay.
