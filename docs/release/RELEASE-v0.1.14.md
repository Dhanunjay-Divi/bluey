# Bluey v0.1.14

This release publishes the current overlay and search-discovery pass.

- Keeps the production overlay excluded from screen capture; capture-visible behavior remains gated to explicit local developer helpers.
- Includes the latest overlay history, attachment, drag, and click-through fixes.
- Publishes the new crawlable Bluey search pages, sitemap, robots.txt, and llms.txt discovery assets.
- Keeps the public alpha macOS-first while preserving the existing signed install/update flow.
