# Bluey v0.1.29

- Changed Hide from "disappear completely" to "minimize to the pill" on the native overlays.
- Updated macOS and Windows shortcut/help copy to say `Minimize to pill / restore`.
- Kept the signed-out auth gate expanded instead of allowing it to collapse into an unusable pill.
