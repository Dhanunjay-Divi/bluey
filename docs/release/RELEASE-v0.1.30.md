# Bluey v0.1.30

- Removed the legacy dashboard `Ctrl+Option+H` global overlay toggle.
- Removed the old macOS full-hide restore toast path.
- Updated install copy to say `Ctrl+Option+B` minimizes/restores Bluey.
- Normal prod overlay/pill capture exclusion remains unchanged.
